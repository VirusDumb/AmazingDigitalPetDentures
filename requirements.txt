# ---- Runtime deps for the HF Space (ZeroGPU, in-process llama.cpp) ----
# The app runs the model IN-PROCESS via llama-cpp-python on ZeroGPU (@spaces.GPU),
# so there is no Modal/OpenAI server and no Agno. Set python_version: '3.12' in
# README.md so the prebuilt wheels below resolve.

# torch ships the CUDA libs llama.cpp needs at runtime (cu128 index).
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.8.0

hf_transfer        # fast GGUF download (HF_HUB_ENABLE_HF_TRANSFER=1, set in model.py)

# Prebuilt llama-cpp-python CUDA wheel (cu124) — avoids a long CUDA build on the Space.
# NOTE: this wheel tracks a specific llama.cpp version; if it can't load Nemotron-3-Nano's
# hybrid Mamba-2 arch ("unknown architecture"), bump to a newer wheel or build from source.
--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu124
llama-cpp-python

gradio==6.16.0     # UI framework — must match README's sdk_version
spaces             # provides @spaces.GPU (no-op off-ZeroGPU, so local dev still imports)
huggingface_hub    # GGUF download (hf_hub_download / Llama.from_pretrained)
